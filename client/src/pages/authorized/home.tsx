import { Header, BlogList } from "@/components";

const Home = () => {
  return (
    <main className="bg-gray-100 font-roboto h-screen">
      <Header />
      <BlogList />
    </main>
  );
};

export default Home;
