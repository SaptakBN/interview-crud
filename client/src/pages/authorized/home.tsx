import { Header, PostList } from "@/components";

const Home = () => {
  return (
    <main className="bg-gray-100 font-roboto h-screen">
      <Header />
      <PostList />
    </main>
  );
};

export default Home;
